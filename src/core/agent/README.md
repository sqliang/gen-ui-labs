# `core/agent`

Agent 运行时。

- `runtime/` — 通用 Agent loop（Observe → Think → Act → Observe）
- `patterns/` — ReAct / Plan-Execute / Reflection / Tool-Use 模板
- `tools/` — 内置工具（render_component、update_state、query_user 等）
- `prompts/` — 模板化 prompt